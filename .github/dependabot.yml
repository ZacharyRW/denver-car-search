version: 2
updates:
  # No package manifests in this repo — the workflow actions are the only
  # dependency surface. They sit on floating major tags, so this is low
  # volume by design: expect a PR only when a major ships.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 2
